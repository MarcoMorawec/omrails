<%= simple_form_for(@pin, html: {class: "form-horzontal"}) do |f| %>
  <%= f.error_notification %>

    <%= f.input :description, as: :text, input_html: { rows: "3" } %>
 

  <div class="form-actions">
    <%= f.button :submit, calss: "btn btn-primary" %>
  </div>
<% end %>
